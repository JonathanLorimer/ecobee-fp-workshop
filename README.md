# ecobee-fp-workshop

```
// if you don't already have typescript installed
npm install -g typescript


git clone git@github.com:JonathanLorimer/ecobee-fp-workshop.git
cd ecobee-fp-workshop
npm install

tsc -w
```
